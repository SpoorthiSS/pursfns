module Main where

import Prelude
import Effect (Effect)
import Effect.Console (logShow)
import Data.Maybe
import Partial.Unsafe (unsafePartial)

-- add :: Int -> Int
-- add num = num + 2



-- maybe1 :: forall a b. b -> (a -> b) -> Maybe a -> b
-- maybe1 b fnc j = maybe b fnc j
-- main = logShow (maybe 3 add ( Nothing))

-- main = logShow (maybe 3 add (Just 1))

-- main = logShow(fromMaybe 2.0 (Nothing))

-- main = logShow (isJust (Nothing))
-- main = logShow (isNothing (Just 2))



